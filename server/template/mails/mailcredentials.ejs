<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= translations.subject %></title>
    <style type="text/css">
        body{
            margin: 0;
            padding: 0;
            min-width: 100;
            font-family: Arial, Helvetica, sans-serif;
            font-size: 16px;
            line-height: 1.5;
            background-color: #FAFAFA;
            color: #222222;
        }
        a{
            color: #000;
            text-decoration: none;
        }
        h1{
            font-size: 24px;
            font-weight: 700;
            line-height: 1.25;
            margin-top: 0;
            margin-bottom: 15px;
            text-align: center;
        }
        p{
            margin-top: 0;
            margin-bottom: 24px;
        }
        table td {
            vertical-align: top;
        }

        .email-wrapper{
            max-width: 600px;
            margin: 0 auto;
            padding: 24px;
            border: 1px solid #ddd;
            border-radius: 8px;
            background-color: #fff;
        }

        .email-header{
            background-color: #0070f3;
            padding: 24px;
            color: #ffffff;
        }
        .email-body{
            padding: 24px;
            background-color: #ffffff;
        }
        .email-footer{
            background-color: #f6f6f6;
            padding: 24px;
        }
        .button{
            display: inline-block;
            background-color: #0070f3;
            color: #ffffff;
            font-size: 16px;
            font-weight: 700;
            text-align: center;
            text-decoration: none;
            padding: 12px 24px;
            border-radius: 4px;
        }
        .text{
            color: #000;
        }
    </style>

</head>
<body>
    <div class="email-wrapper">
        <div class="email-header">
            <h1><%= translations.welcome_message %></h1>
        </div>
        <div class="email-body">
            <p class="text"><%= translations.greeting %> <%= newUser.name %>,</p>
            <p class="text"><%= translations.registration_success %></p>
            <p><%= translations.user_id %>: <%= UserId %></p>
            <p><%= translations.password %>: <%= Password %></p>
            <p class="text"><%= translations.contact_support %></p>
        </div>
    </div>
</body>
</html>