import express from 'express';
// import { checkConnection, getMail } from '../controllers/cron/cron.controller';

const cronRouter = express.Router();

// cronRouter.post('/check-connection', checkConnection);
// cronRouter.post('/get', getMail);

export default cronRouter;
